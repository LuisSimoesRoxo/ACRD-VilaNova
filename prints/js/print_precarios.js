document.addEventListener('DOMContentLoaded', () => {const ctx=window.opener&&window.opener.__ACRD_PRINT_CONTEXT__; if(!ctx){document.body.innerHTML='<p class="page">Sem contexto de impressão.</p>'; return;} document.getElementById('title').textContent=ctx.title||'Preçário'; document.getElementById('meta').textContent=`Impresso em ${new Date(ctx.printedAt).toLocaleString('pt-PT')}`; document.getElementById('bodyRows').innerHTML=(ctx.rows||[]).map(r=>`<tr><td>${r.data||'N.A.'}</td><td>${r.nome_produto||'N.A.'}</td><td>${r.categoria||'N.A.'}</td><td class="money">${window.opener.App.formatEuro(r.valor)}</td></tr>`).join('');});