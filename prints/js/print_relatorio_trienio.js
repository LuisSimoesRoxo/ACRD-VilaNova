document.addEventListener('DOMContentLoaded', () => {const ctx=window.opener&&window.opener.__ACRD_PRINT_CONTEXT__; if(!ctx){document.body.innerHTML='<p class="page">Sem contexto de impressão.</p>'; return;} document.getElementById('title').textContent=ctx.title||'Relatório Triénio'; document.getElementById('meta').textContent=`Impresso em ${new Date(ctx.printedAt).toLocaleString('pt-PT')}`; document.getElementById('bodyRows').innerHTML=(ctx.rows||[]).map(r=>`<tr><td>${r.ano}</td><td class="money">${window.opener.App.formatEuro(r.banco)}</td><td class="money">${window.opener.App.formatEuro(r.caixa)}</td><td class="money">${window.opener.App.formatEuro(r.total)}</td><td class="money">${window.opener.App.formatEuro(r.receita)}</td><td class="money">${window.opener.App.formatEuro(r.despesa)}</td><td class="money">${window.opener.App.formatEuro(r.saldo)}</td></tr>`).join('');});